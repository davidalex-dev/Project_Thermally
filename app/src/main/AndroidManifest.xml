<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uc.thermally">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_Thermally">
        <activity android:name=".HistoryActivity"></activity>
        <activity android:name=".ColdActivity" />
        <activity android:name=".WarmActivity" />
        <activity android:name=".HotActivity" />
        <activity android:name=".ConvertInfoActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ConvertActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".AboutActivity" />
    </application>

</manifest>